//
// Copyright (c) Vatsal Manot
//

import SwiftUI

/// A marker protocol for views that essentially just wrap an `AnyView`.
///
/// Potential bottleneck for performance and a potential refactoring task once I understand SwiftUI graph stuff better.
public protocol _AnyViewWrapper {
    
}
